import { CaixaAltaPipe } from './caixa-alta.pipe';

describe('CaixaAltaPipe', () => {
  it('create an instance', () => {
    const pipe = new CaixaAltaPipe();
    expect(pipe).toBeTruthy();
  });
});
